{
  "name": "@pasteking/cli",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "pasteking": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@pasteking/sdk": "workspace:*",
    "@pasteking/types": "workspace:*",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "@pasteking/config": "workspace:*",
    "@types/node": "^25.5.2",
    "rimraf": "^6.1.3",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
